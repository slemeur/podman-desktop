# Launch bot
name: Downloads Count

on:
  workflow_dispatch:

jobs:
  echo-input:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v6
        with:
          script: |
            console.log(context);
            const test = await github.rest.repos.listReleases({
                owner: 'containers',
                repo: 'podman-desktop'
            })

            console.log(test.data.length);